{
  "name": "http-equiv-refresh",
  "description": "Parse an HTML meta refresh value.",
  "version": "2.0.0",
  "license": "MIT",
  "author": "Steven Vachon <contact@svachon.com> (https://svachon.com)",
  "repository": "github:stevenvachon/http-equiv-refresh",
  "browser": "index-es5.js",
  "devDependencies": {
    "@babel/cli": "^7.4.3",
    "@babel/core": "^7.4.3",
    "@babel/preset-env": "^7.4.3",
    "chai": "^4.2.0",
    "mocha": "^6.1.4",
    "nyc": "^14.0.0"
  },
  "engines": {
    "node": ">= 6"
  },
  "scripts": {
    "posttest": "nyc report --reporter=text-summary --reporter=html",
    "prepublishOnly": "npm test && babel index.js --out-file=index-es5.js --presets=@babel/env --source-maps",
    "test": "nyc --silent mocha test.js --reporter=spec --check-leaks --bail"
  },
  "files": [
    "index.js",
    "index-es5.js",
    "index-es5.js.map"
  ],
  "keywords": [
    "html",
    "http-equiv",
    "meta",
    "metadata",
    "parse",
    "refresh"
  ]
}
